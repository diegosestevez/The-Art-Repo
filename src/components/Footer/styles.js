import {makeStyles} from '@material-ui/core/styles';

export default makeStyles(()=>({
    footer:{
        // position: 'fixed',
        bottom: '0',
        width: '100%',
        // height: '100%',
        backgroundColor: 'black',
        color: 'white',
    },
}));